# cudloun

<p align="center">
  <img src="cudloun.png" alt="Cudloun" width="360">
</p>

Babeta extensions framework for Okoun userscripts.

Install:

https://raw.githubusercontent.com/hanenashi/cudloun/main/cudloun.user.js


## Current shape

- `cudloun.user.js` is the installable seed userscript.
- `modules/core.js` boots the framework from GitHub raw URLs.
- `modules.json` lists system modules and feature modules.
- `modules/sys-logger.js` owns logging controls.
- `modules/sys-feedback.js` owns Firestore-backed per-feature feedback threads.
- `modules/sys-menu.js` owns the Babeta avatar menu and Cudloun hub.
- `modules/settoun.js` owns framework settings for Cudloun itself.
- `modules/post-tweaks.js` owns board post layout tweaks.
- `modules/containers.js` lists standalone live demos from `containers.json`.
- `containers/` holds standalone tweak demos that can run from Cudloun or from a console loader.

The seed loader fetches modules with a `?v=Date.now()` cache buster while the framework is under active development.

The current skeleton exposes a Cudloun entry in Babeta's avatar menu, opens a module hub, stores module enable switches in `localStorage`, includes a Debug view with recent logs, has Settoun for framework-level settings, and shows Firestore-backed feedback threads for modules and containers.

`modules/sys-babeguts.js` exposes a small Babeta DOM dictionary at `window.Cudloun.babeguts`. It starts with board-page helpers for visible posts, post parts, visible MUI menus, and a compact `inspect()` snapshot for live-page tweaking sessions.

Keep `babegun.md` updated as the quick field guide for Babeta live-DOM tweaking sessions.

## Containers

Containers are small standalone UI experiments. They are useful for trying a tweak on a live Babeta page before deciding whether it should become a full Cudloun module or an upstream Babeta change.

A container can be:

- run from the Cudloun `Containers` module
- stopped again from the same panel
- shared as a one-line console loader, so someone can preview the tweak without installing a userscript

Current containers:

- `favorite-pill-colors`: colors unread counters on `https://babeta.okoun.cz/favorites`.

Container files live in `containers/` and are listed in `containers.json`.

For safety, Cudloun only accepts repo-local container paths shaped like `containers/name.container.js`, and each container must match the SHA-256 hash listed in `containers.json` before it runs. Console loaders generated by Cudloun include the same hash check.

## Feedback Backend

Cudloun feedback/discussion threads are per-framework, per-module, and per-container boards where normal users can try UI changes and leave ideas before anything is proposed upstream.

Firebase project:

```text
murkypond-vault-fc61c
```

Prepared Firestore roots:

```text
cudlounMeta/feedback
cudlounThreads/framework_cudloun
cudlounThreads/module_settoun
cudlounThreads/module_containers
cudlounThreads/module_post-tweaks
cudlounThreads/container_favorite-pill-colors
```

Each `cudlounThreads/{threadId}` document owns a `messages` subcollection. The intended client message shape is:

```js
{
  schemaVersion: 1,
  author: "Babeta visible username",
  text: "Feedback text",
  ts: 1710000000000,
  route: "/boards/nepotrebny_pokus",
  cudlounVersion: "0.4.16",
  userAgentHint: "mobile or desktop hint"
}
```

The visible Babeta username is convenience identity, not authentication. Before opening this to real users, Firebase rules should allow public reads and message creates only, reject client edits/deletes, validate the allowed fields, and cap feedback text length.

## Version 0.4.16 TL;DR

- Moved `post-tweaks` out of `containers/` and into `modules/post-tweaks.js`.
- Added Post Tweaks as a normal Cudloun module with the same floating control panel and existing local settings key.
- Removed Post Tweaks from the container catalog and prepared its module feedback thread.

## Version 0.4.15 TL;DR

- Added `Cudloun.babeguts.currentUser()` and `currentUserCandidates()` for live Babeta username detection.
- Updated feedback author detection to use desktop top avatar and mobile bottom navigation avatar instead of broad page controls.
- Ignored bad author placeholders such as `Hledat v klubu` when restoring feedback author state.

## Version 0.4.14 TL;DR

- Added `sys-feedback`, a Firestore REST client for Cudloun feedback threads.
- Added Feedback panels to module detail pages and container cards.
- Added `firestore.googleapis.com` to the userscript connect list.

## Version 0.4.13 TL;DR

- Prepared Firestore metadata and empty Cudloun feedback threads for framework, module, and container discussions.
- Documented the intended feedback message shape and Firebase safety assumptions.

## Version 0.4.12 TL;DR

- Made the Cudloun hub draggable from its header.
- Added a collapse/expand button so the hub can shrink to a compact header while staying where it was moved.

## Version 0.4.11 TL;DR

- Stabilized the `post-tweaks` Avatar opens menu path so stale native bottom drawers are suppressed.
- The avatar-opened post menu no longer darkens or blurs the page behind it.
- Added Copy log and Export log actions to the Cudloun Debug view.
- Updated the `post-tweaks` container manifest hash and bumped the container manifest to `0.1.9`.

## Version 0.4.10 TL;DR

- Added an Avatar opens menu option to `post-tweaks`.
- When enabled, post meatball buttons are hidden and clicking a post avatar opens the native post actions as a compact popout.
- Added Export and Import controls for shareable `post-tweaks` settings JSON.
- Updated the `post-tweaks` container manifest hash and bumped the container manifest to `0.1.8`.

## Version 0.4.9 TL;DR

- Made the `post-tweaks` control panel more compact on phones.
- Added internal scrolling so the panel stays within the viewport.
- Added dragging from the panel header, clamped to the visible screen.
- Updated the `post-tweaks` container manifest hash and bumped the container manifest to `0.1.7`.

## Version 0.4.8 TL;DR

- Added `sys-babeguts`, a Babeta board-page DOM dictionary for live tweaking work.
- Updated `post-tweaks` to use Babeguts post/menu helpers when Cudloun is present, while keeping standalone fallbacks.
- Updated the `post-tweaks` container manifest hash and bumped the container manifest to `0.1.6`.

## Version 0.4.7 TL;DR

- Fixed `post-tweaks` header-menu reply mode for Babeta's current post menu DOM.
- Added a `post-tweaks` Pop out post menu checkbox that anchors the post meatball menu to the clicked button instead of leaving it as a bottom sheet.
- Updated the container manifest hash and bumped the container manifest to `0.1.5`.

## Version 0.4.6 TL;DR

- Changed `post-tweaks` reply-in-menu mode to use Babeta's native post menu instead of adding a second custom menu button.
- Updated the container manifest hash and bumped the container manifest to `0.1.3`.

## Version 0.4.5 TL;DR

- Added `post-tweaks` controls for moving the reply button to the post header or a small header menu.
- Added an option to move `Re:` reply metadata under the current post timestamp and hide the empty bottom action row.
- Tightened mobile post card padding so text has matching left, right, and bottom breathing room.

## Version 0.4.4 TL;DR

- Added a Card inset slider to `post-tweaks` so mobile post cards can be narrowed and centered.
- Fixed `post-tweaks` avatar-in-header mode so multi-paragraph posts and inserted images no longer collapse into the avatar column.

## Version 0.4.3 TL;DR

- Refresh from fullscreen now remembers the fullscreen intent and shows a one-tap restore prompt after reload.

## Version 0.4.2 TL;DR

- Removed the Nepotrebny pokus toy module.
- Added Settoun as Cudloun's framework settings module.
- Added a Show fullscreen toggle that shows or hides the avatar menu Full/Refresh quick actions.
- Added Refresh next to Fullscreen for mobile fullscreen mode, where pull-to-refresh is unavailable.

## Version 0.4.1 TL;DR

- Fixed mobile drawer closing so Cudloun no longer leaves Babeta's avatar menu hidden after opening the hub.

## Version 0.4.0 TL;DR

- Added proper mobile support for Babeta bottom-sheet menus.
- Cudloun now injects into Babeta mobile drawer menus on phones/tablets.
- Hub layout was redesigned for narrow mobile screens.
- Mobile module list is now horizontal instead of desktop sidebar squeeze mode.
- Improved viewport handling and mobile scrolling behavior.
- Added better mobile observer handling for dynamic Babeta UI changes.
- Desktop avatar menu injection still works as before.

Desktop browser mobile emulation (`F12` -> `Ctrl+Shift+M`) is now good enough for most Cudloun mobile UI debugging before testing on real Android devices.
